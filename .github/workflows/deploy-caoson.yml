# Reference:
# 1. https://github.com/actions/starter-workflows/blob/main/deployments/azure-webapps-node.yml
name: Deploy for release version

on:
  push:
    branches:
      - dev/caoson # Deploy when code is pushed to 'develop'

env:
  WEBAPP_NAME: data-lake # set this to your application's name
  WEBAPP_PACKAGE_PATH: "." # set this to the path to your web app project, defaults to the repository root
  NODE_VERSION: "20.x" # set this to the node version to use

# permissions:
#   contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show Working Directory
        run: pwd

      - name: Create an .env file in frontend for building image
        run: |
          echo "VITE_API=${{ secrets.VITE_API }}" > ./fe-modul-report/.env

      - name: List Files
        run: ls -laR

      - name: Run Makefiles with Secrets to build anh push docker images to dockerhub
        env:
          DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
          DOCKER_HUB_ACCESS_TOKEN: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
        run: |
          make staging
          ls -al .env
          cat .env
          cp .env deployment/release/

      - name: Ensure target directory exists
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.CAOSON_HOST }}
          username: ${{ secrets.CAOSON_USER }}
          key: ${{ secrets.CAOSON_SSH_KEY }}
          port: ${{ secrets.CAOSON_PORT }}
          script: |
            mkdir -p /home/${{ secrets.CAOSON_USER }}/data-lake/deployment/release

      - name: Transfer docker-compose file to VPS for deployment
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.CAOSON_HOST }}
          username: ${{ secrets.CAOSON_USER }}
          key: ${{ secrets.CAOSON_SSH_KEY }}
          port: ${{ secrets.CAOSON_PORT }}
          source: deployment/release/*
          target: "/home/${{ secrets.CAOSON_USER }}/data-lake/deployment/release"
          strip_components: 2 # Removes "deployment/release/" folder path!

      - name: Clean up containers and stuff via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.CAOSON_HOST }}
          username: ${{ secrets.CAOSON_USER }}
          key: ${{ secrets.CAOSON_SSH_KEY }}
          port: ${{ secrets.CAOSON_PORT }}
          script: |
            # Change to release deployment directory
            cd /home/${{ secrets.CAOSON_USER }}/data-lake/deployment/release 
            # Debug info
            pwd
            ls -al
            docker image prune -f
            docker compose -f docker-compose.yaml down # always down first
            docker ps

      - name: Deploy to release via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.CAOSON_HOST }}
          username: ${{ secrets.CAOSON_USER }}
          key: ${{ secrets.CAOSON_SSH_KEY }}
          port: ${{ secrets.CAOSON_PORT }}
          script: |
            ### Change to release deployment directory
            cd /home/${{ secrets.CAOSON_USER }}/data-lake/deployment/release 
            ### Clean up unused images
            docker image prune -f # clean up unused images
            ### Provide permissions and start the application
            chmod +x start-app.sh
            ./start-app.sh
